\clearpage
\section*{Problem 7}
\addcontentsline{toc}{section}{Problem 7}
\markboth{Problem 7}{Problem 7}

\textbf{Problem Statement (Textbook 4.6.1b):} 

Program the Gauss-Seidel method and test it on the following system:

\begin{align*}
3x + y + z &= 5 \\
3x + y - 5z &= -1 \\
x + 3y - z &= 3
\end{align*}

Analyze what happens when these systems are solved by simple Gaussian elimination without pivoting.

\noindent\rule{\textwidth}{0.4pt}

\vspace{1em}

\textbf{Solution:}

\textbf{System Matrix:}
$$A = \begin{bmatrix} 3 & 1 & 1 \\ 3 & 1 & -5 \\ 1 & 3 & -1 \end{bmatrix}, \quad 
b = \begin{bmatrix} 5 \\ -1 \\ 3 \end{bmatrix}$$

\textbf{True Solution} (using direct method): $\boxed{x = y = z = 1}$

\subsection*{Part 1: Gauss-Seidel Method}

The Gauss-Seidel iteration is:
\begin{align*}
x^{(k+1)} &= \frac{1}{3}(5 - y^{(k)} - z^{(k)}) \\
y^{(k+1)} &= \frac{1}{1}(-1 - 3x^{(k+1)} + 5z^{(k)}) \\
z^{(k+1)} &= \frac{1}{-1}(3 - x^{(k+1)} - 3y^{(k+1)})
\end{align*}

\textbf{Diagonal Dominance Check:}
\begin{itemize}
\item Row 1: $|a_{11}| = 3 > |1| + |1| = 2$
\item Row 2: $|a_{22}| = 1 \not> |3| + |-5| = 8$
\item Row 3: $|a_{33}| = |-1| = 1 \not> |1| + |3| = 4$
\end{itemize}

$\boxed{\text{Matrix is NOT strictly diagonally dominant}}$

\textbf{Convergence Results} (see \texttt{hw05\_p07.jl}):
\begin{itemize}
\item Starting from $(1, 1, 1)$: \textbf{Converges in 1 iteration} (already at solution!)
\item Starting from $(0.999, 0.999, 0.999)$: \textbf{Diverges} (NaN)
\item Starting from $(1.01, 1.01, 1.01)$: \textbf{Diverges} (NaN)
\item Starting from $(0.9, 0.9, 0.9)$ or $(1.1, 1.1, 1.1)$: \textbf{Diverges} (NaN)
\item Starting from $(0, 0, 0)$, $(5, -1, 3)$, or $(10, 10, 10)$: \textbf{Diverges} (NaN)
\end{itemize}

\textbf{Key Observation:} The method only "converges" when starting exactly at the solution. Even tiny perturbations (0.001 away) cause catastrophic divergence to NaN. This demonstrates \textbf{complete instability}.

\subsection*{Part 2: Gaussian Elimination without Pivoting}

Forward elimination proceeds:

\textbf{Step 1:} Eliminate column 1 using pivot $a_{11} = 3$:
$$\begin{bmatrix} 3 & 1 & 1 \\ 0 & 0 & -6 \\ 0 & 8/3 & -4/3 \end{bmatrix}
\begin{bmatrix} x \\ y \\ z \end{bmatrix} = 
\begin{bmatrix} 5 \\ -6 \\ 4/3 \end{bmatrix}$$

\textbf{Step 2:} Attempt to eliminate column 2 using pivot $a_{22}$:
$$\boxed{a_{22} = 0 \text{ (ZERO PIVOT!)}}$$

\textbf{Analysis:} 
\begin{itemize}
\item Gaussian elimination \textbf{fails} due to zero pivot at position $(2,2)$
\item Rows 2 and 3 need to be swapped (partial pivoting required)
\item Without pivoting, the method cannot proceed
\item The system has determinant $\det(A) = 48 \neq 0$, so the matrix is invertible
\item Failure is due to \textit{method limitation}, not matrix singularity
\end{itemize}

\textbf{Summary:} Both methods encounter difficulties with this system:
\begin{itemize}
\item \textbf{Gauss-Seidel:} Diverges for most starting points (not diagonally dominant)
\item \textbf{Gaussian Elimination:} Fails without pivoting (zero pivot encountered)
\end{itemize}

\textbf{Implementation:} See Appendix A for complete Julia code listings:
\begin{itemize}
\item Gauss-Seidel method: Appendix \ref{app:p07-gauss-seidel}
\item Gaussian elimination: Appendix \ref{app:p07-gaussian-part1} and \ref{app:p07-gaussian-part2}
\end{itemize}

\vspace{2em}
\noindent\rule{\textwidth}{0.4pt}
